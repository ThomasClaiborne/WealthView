import { Component } from '@angular/core';

@Component({
    selector: 'app-dashboard',
    standalone: true,
    template: `
        <h2>Dashboard</h2>
        <p class="text-muted">Coming in the next slice.</p>
    `
})
export class Dashboard {}